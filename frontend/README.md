# Frontend (Vite + React)

Setup

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

This is a starter skeleton. The UI will be expanded with creative/unique designs and full role-aware flows in subsequent steps.
